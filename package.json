{
  "name": "dexis-nails",
  "version": "2.0.0",
  "description": "Dexis Nails — Asistente Dexis con DeepSeek",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.11.3",
    "@neondatabase/serverless": "^0.9.0"
  },
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Build OK — frontend estático'"
  }
}